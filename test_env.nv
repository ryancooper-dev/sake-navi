// Test 3: Check environment and basic process info
use std.env;

fn main() throws {
    // Environment variables
    let home = env.get("HOME");
    println(`HOME: ${home || "not set"}`);
    
    // Args
    let args = env.args();
    println(`Args count: ${args.len()}`);
}

// Test 1: Check std.process module
use std.process;

fn main() throws {
    // Test getpid
    let pid = process.getpid();
    println(`Current PID: ${pid}`);
    
    // Test CPU count
    let cpus = process.cpu_count();
    println(`CPU cores: ${cpus}`);
}
